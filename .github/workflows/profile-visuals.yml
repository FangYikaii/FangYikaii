name: Generate Profile Visuals

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: "20 18 * * *"

permissions:
  contents: write

jobs:
  build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate 3D contribution heatmap
        uses: yoshi389111/github-profile-3d-contrib@latest
        env:
          # Set PROFILE_GITHUB_TOKEN to a PAT that can read SynlysAI repositories.
          GITHUB_TOKEN: ${{ secrets.PROFILE_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          USERNAME: FangYikaii
          MAX_REPOS: 300

      - name: Generate SynlysAI PR summary
        env:
          GITHUB_TOKEN: ${{ secrets.PROFILE_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          ORG: SynlysAI
          USERNAME: FangYikaii
          OUTPUT: profile-3d-contrib/synlysai-pr-summary.svg
        run: python scripts/generate_synlysai_pr_summary.py

      - name: Commit generated visual assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile-3d-contrib
          git diff --cached --quiet && exit 0
          git commit -m "更新 GitHub 主页 3D 热力图"
          git push
